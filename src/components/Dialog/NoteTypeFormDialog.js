// import React from 'react'
// import { connect } from 'react-redux'
// import { Dialog } from '@material-ui/core'
// // import NoteTypeForm from '../NoteTypeForm'

// const NoteTypeFormDialog = ({
//   open,
//   closeDialog,
//   dispatchAction,
//   noteTypeId,
// }) => (
//   <Dialog open={open}>
//     <NoteTypeForm id={noteTypeId} cancel={closeDialog} onSubmit={closeDialog} />
//   </Dialog>
// )

// const mapDispatchToProps = (dispatch, { action, closeDialog }) => ({
//   dispatchAction: () => {
//     dispatch(action)
//     closeDialog()
//   },
// })

// export default connect(
//   null,
//   mapDispatchToProps
// )(NoteTypeFormDialog)
